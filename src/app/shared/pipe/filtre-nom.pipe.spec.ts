import { FiltreNomPipe } from './filtre-nom.pipe';

describe('FiltreNomPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreNomPipe();
    expect(pipe).toBeTruthy();
  });
});
